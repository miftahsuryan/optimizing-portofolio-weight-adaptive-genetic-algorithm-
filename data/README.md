# Local data

Place local price datasets in this directory. Dataset files are ignored by Git
because research and market data can be large or have redistribution limits.

The default configuration expects `data/prices.csv` with these columns:

```text
Date,ticker,Close
```
